/**
 * 1. Use the .toUpperCase() method to log the string Codecademy to the console in all capital letters.
 */

console.log("Codecademy".toUpperCase());

/**
 * 2. Use the JavaScript documentation to find a string method that trims the whitespace at the beginning and end of a string.

Use the method to remove the whitespace at the beginning and end of the following statement (also in app.js):
 */

console.log('    Remove whitespace   '.trim());