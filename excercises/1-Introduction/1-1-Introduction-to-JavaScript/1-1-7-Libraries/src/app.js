/**
 * 1. Inside of a console.log, create a random number with Math.random, then multiply it by 100.
 */

console.log(Math.random() * 100);

/**
 * 2. Now, utilize Math.floor to make the output a whole number.

Inside the console.log you wrote in the last step, put Math.random() * 100 inside the parentheses of Math.floor.
 */

console.log(Math.floor(Math.random() * 100));

/** 
 * 3. Find a method in the JavaScript math library that returns the smallest integer greater than or equal to a decimal number. 
 * 
 * Use this method with the number 43.8. Log the answer to the console. */

console.log(Math.ceil(43.8));

/** 
 * 4. Use the JavaScript documentation to find a method in the Number library that checks if a number is an integer.
 *  
 * */

console.log(Number.isInteger(2018));